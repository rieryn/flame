name: parallax
description: Flame sample game showcasing the parallax features

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.0.0-rc1
  provider:
  liquid_progress_indicator: ^0.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/
